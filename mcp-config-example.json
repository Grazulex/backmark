{
  "mcpServers": {
    "backmark": {
      "command": "backmark",
      "args": [],
      "env": {
        "BACKMARK_PATH": "/home/jean-marc-strauven/Dev/Backmark"
      }
    }
  },
  "agents": {
    "backmark-agent": {
      "name": "Backmark Task Manager",
      "description": "Specialized agent for managing tasks using Backmark CLI",
      "promptFile": ".claude/agents/backmark-agent.md",
      "tools": ["bash"],
      "autoActivate": {
        "when": "backlog directory exists",
        "pattern": "backlog/config.yml"
      }
    }
  }
}
